# 项目术语

本文定义硬件设计与文档使用的统一术语。术语只描述概念，不记录电路参数。

## 现场侧

连接外部传感器、执行器、总线或电源的电路与网络。现场侧可能承受更长的线缆、更高的电压和更强的干扰。

## 控制器侧

连接 STM32、ESP32-S3、板载逻辑器件和低压电源的电路与网络。

## 通道

一条独立采集或控制路径。一个通道包含现场连接、信号调理、控制器接口和对应的软件状态。

## 模块

具有明确输入、输出和职责的硬件或软件单元。模块的界面说明调用方必须知道的信号、协议、约束和错误模式。

## 接口

两个模块之间的完整连接契约。接口包含引脚、电气范围、时序、协议、配置和故障行为。

## 控制器域

STM32 或 ESP32-S3 各自负责的运行范围。STM32 域处理实时采集和控制。ESP32-S3 域处理显示、网络、配置和 OTA。

## 监测

对电源、温度、通信或设备状态的周期采样与判断。监测产生状态值、报警或维护事件。
